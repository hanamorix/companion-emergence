{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "default permissions for nellface",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}

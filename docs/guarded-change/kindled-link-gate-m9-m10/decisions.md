# Gate log — kindled-link gate m9/m10

Append-only. One line per gate (4, 7, 8): gate#, worst severity, route, rationale.

- **Gate 4 (plan red-team)** — worst: **major** (2). Route: **fold into plan revision, proceed to build** (operator call — both majors are reviewer-confirmed non-blockers that fail-toward-hold; resolution is a recorded design call + test additions + a pattern narrowing, not an approach change). Stage 6 (cold code review of the real tests) is the independent challenge of the resolution. Findings carried forward: (1) JWT FP on base64-of-JSON relationship_hint — DESIGN CALL: keep JWT, fail-toward-hold is correct gate bias; correct the overstated claim + add accepted-hold test. (2) all-caps AWS prose collision — narrow AWS to AKIA|ASIA (the only credential prefixes; AGPA/AIDA/AROA/… are resource-id prefixes, out of m9's credential scope) + add caps corpus line. Minors folded: L51→L52 ref, JWT short-segment test, whsec_ omission rationale, C-m10-4 reads budget at same `now`. Operator: Hana/Nell.

- **Gate 7 (code red-team)** — worst IN-SCOPE: **minor** (missing Stripe FP-corpus line — fixed in place). Route: **fix-and-proceed to harness**. The reviewer's one **major** is a PRE-EXISTING email-regex ReDoS (`privacy_gate.py:30`, `[\w.+-]+@...`) NOT touched by this diff; reviewer explicitly said "do not gate this change on it." Filed as a new defer (kindled ledger — email-regex ReDoS / _payload_text length cap). All 6 new m9 regexes verified ReDoS-clean to 50K-char inputs. Invariant re-verified by the cold reviewer: transcript_summary prompt-only; _apply_budget send-only; depletion-before-tighten. Operator: Hana/Nell.
- **Gate 8 (harness)** — **clean**. Conformance: all C-* PASS. Regression: R1 kindled 307 (was 264), R2 ruff clean, R3 full backend 3801 passed (advisory, no collateral break). Layer-2 cost/cache N/A (off chat path). Route: **done**. The pre-existing email ReDoS remains a separate defer.

# Gate log — kindled-link relay abuse/quota (#48)

Append-only. One line per gate (4, 7, 8): gate#, worst severity, route, rationale.

- **Gate 4 (plan red-team)** — worst: **BLOCKER** (C-5). Route: **return to 1/2, corrected direction, proceed** (operator call — the fix is forced by store-and-forward protocol semantics, not a user preference; #48 goal unchanged). Blocker: registration-gating `/envelope` drops deposits to not-yet-self-registered recipient mailboxes (verified: existing `test_push_then_fetch` pushes to unregistered `mbx_b`; recipients self-register lazily on their own tick; in-memory relay restart races) → DROP C-5, bound keyspace via global distinct-mailbox cap on first-touch instead. Compounding MAJOR: new 4xx codes KeyError-crash the brain (`relay_client._post` catches only TransportError, callers do `.json()["nonce"]` unchecked) → ADD `relay_client.py` fail-soft to scope (C-12). Minors folded: cap body-then-hint (sentinel survives), rate-limit key by mailbox-id-else-IP + prune-all, 413-before-mailbox-work, queue `>=`, fetch-egress residual named. Re-running stage 3 on the redesign. Operator: Hana/Nell.
- **Gate 4 (re-review, 2nd bounce)** — worst: **MAJOR ×3**. Route: **STOP for human (iteration-cap: 2nd gate-4 bounce on the keyspace-bound finding class)**. The blocker-fix (open /envelope + single global mailbox cap) is correct on store-and-forward but introduces: (1) keyspace-exhaustion onboarding lockout (needs split registered/unregistered pools + LRU evict); (2) cross-tenant push DoS (rate-limiter keyed on mailbox_id = recipient on open /envelope → key on sender host instead); (3) C-12 fail-soft incomplete (poll_and_ingest fetch() at tick.py:83 unguarded → 4xx aborts tick; needs poll guard + 404 re-register). All citations verified. C-10 ReDoS body-cap + payload.body/sentinel claims clean. Converging, not livelocked — but design grew past the initial sketch; surfacing the scope decision to Hana (full vs narrow-defer-keyspace vs pause). Operator: Hana/Nell.
- **Gate 7 (code red-team)** — worst: **MAJOR ×2** → **fixed in build, re-review**. (1) Rate-before-auth on /mailbox/fetch|ack keyed on the attacker-suppliable victim mailbox_id = cross-tenant DoS → FIX: rate-limit ALL endpoints by client HOST (the requester), uniformly (supersedes the Rev-2 mailbox_id keying for mailbox endpoints; NAT-sharing is the accepted LAN tradeoff). Regression pin `test_c8b_fetch_rate_keyed_by_host_not_mailbox_id`. (2) C-15 self-heal "untested/unimplemented" — REVIEWER MISS: supervisor.py:664 calls relay.register() every tick (guarded) BEFORE run_kindled_link_tick, so a relay restart re-registers before poll. Verified + pinned `test_c15_reregister_after_relay_restart_restores_challenge`. Split-pool eviction (never drops mail), sender-keyed push, poll fail-soft all verified CLEAN by the reviewer. 331 kindled green, ruff clean. Operator: Hana/Nell.
- **Gate 7 (re-review, post host-keying)** — cross-tenant rate DoS **CLOSED** (verified, not relocated). Worst: **MAJOR (residual queue-fill DoS) — ACCEPTED/DEFERRED** (operator call): an open-/envelope attacker can slowly fill a victim's 256 queue (blocks NEW senders, victim still reads own mail) — bounded, self-healing on ack, host-rate-limited, scoped to trusted-LAN; named in the Phase-7b public-relay checklist (sender-auth / per-sender queue quota). Two MINORs fixed: stale module/test docstrings ("mailbox endpoints by mailbox_id" → host-keyed) corrected; C-15 brain-half accepted as structurally satisfied by supervisor.py:664 (pre-existing guarded per-tick register) with the relay-side pin. Route: proceed to stage 8. Operator: Hana/Nell.
- **Gate 8 (harness)** — **clean**. Conformance all PASS. Regression: R1 kindled 331 (was 264), R2 ruff clean, R3 full backend 3825 passed (advisory, no collateral break). Layer-2 N/A. One accepted residual (queue-fill DoS → Phase-7b checklist). Route: **done**. Operator: Hana/Nell.

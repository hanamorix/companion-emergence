# 1.5 — Acceptance criteria (persisted-cadence, defer #21)

All criteria are **automated** (true/false from tests, grep, or diff). Stage 8
conformance checks each.

| # | Criterion | Check | Gating |
|---|---|---|---|
| **C1** | `brain/bridge/persisted_cadence.py` exists exposing `CadenceState`, `load_cadence`, `save_cadence`, `is_due`, `advance`. Helper unit tests pass: missing/corrupt/non-dict/bad-ts → due-now; future `next_at` not due; past `next_at` due; `advance` sets `next_at == now+interval`; save→load round-trip leaves no `.tmp`; sleep canary (future state not due until wall-clock passes it). | `uv run pytest tests/unit/brain/bridge/test_persisted_cadence.py -q` → PASS | yes |
| **C2** | Each of the three cadences fires from a persisted past-due state on a fresh `run_folded`, and rewrites its state file with a future `next_at`. (Through-path, mirrors `test_supervisor_soul_cadence.py`.) | `uv run pytest tests/bridge/test_supervisor_cadence_persisted.py -q` → PASS (3 tests) | yes |
| **C3** | No monotonic cadence locals remain for the three converted cadences. | `grep -n "last_finalize_at\|last_maintenance_at\|last_voice_reflection_at" brain/bridge/supervisor.py` → empty | yes |
| **C4** | No surrounding-system regression. | `uv run pytest -q -p no:randomly` → ≥3729 passed, 0 failed; `uv run ruff check brain/ tests/` clean | yes |
| **C5** | Behavioural-shift guard: the three tick *bodies* are unchanged — only the cadence guard + footer differ. The tick call sites keep identical arguments (`_run_finalize_tick(...)` arg list, `forgetting_run_pass(...)` + narrative + file-pending sweep, `_run_voice_reflection_tick(persona_dir, provider, event_bus)`). | `git diff` inspection: changed hunks are confined to the init lines, the three `if … :` guards, and the three footers (advance+save). No edit inside a tick body. | yes |
| **C6** | Migration: a persona with no cadence state files fires each cadence once on first boot (due-now), then settles. | Covered by C1 (`is_due(CadenceState(None))` is True) + C2 (a due state fires + advances). No separate run needed. | yes |

**Regression (relative).** The Layer-2 standing metrics (cost/cache/tool) are
orthogonal to cadence timing (stage 0) → not applicable; C4 (full suite) is the
regression backstop. No advisory metrics apply.

**Conformance oracle for stage 8 = C1–C6 above.**

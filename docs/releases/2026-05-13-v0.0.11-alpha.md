# v0.0.11-alpha.1 release notes — 2026-05-13

## What changed

This release packages the initiate-physiology arc:

- v0.0.9 initiate substrate: autonomous outbound candidate queue, review cadence,
  voice-edit proposals, draft-space fallback, UI banners, and operator CLI views.
- v0.0.10 D-reflection: editorial filtering before composition, draft-space
  demotion, reflex/research event sources, D telemetry, and `nell initiate d-stats`.
- v0.0.11 adaptive-D: calibration rows, calibration closer, drift telemetry,
  recall-resonance memory activation, recent-conversation excerpting, and real
  Haiku-backed research topic-overlap scoring.

## Local release-check findings

Ran on macOS arm64 from `the project root/.worktrees/v011-adaptive-d`
before merging the release branch back to `main`:

- `uv run ruff check .` — passed
- `uv run pytest -q` — 1972 passed
- `cd app && pnpm test` — 56 passed across 8 files
- `cd app/src-tauri && cargo test` — 28 passed
- `cd app && pnpm tauri build` — produced `.app` and `.dmg`
- `hdiutil verify` on the DMG — valid
- `codesign --verify --deep --strict` on the app bundle — valid ad-hoc signature
- Bundled Python / `nell --help` smoke — passed

## Known non-blocking findings

- Gatekeeper rejects the local app because this alpha is ad-hoc signed and not
  notarized. This is expected until the project uses a paid Apple Developer ID.
- Tauri warns that the bundle identifier `com.companion-emergence.app` ends in
  `.app`; rename before a notarized/stable public release.
- macOS x86_64 remains source-build-only until a reliable Intel runner is added.

## Release target

- Package version: `0.0.11`
- Git tag: `v0.0.11-alpha.1`
- Public sync path: `bash .public-sync/sync-to-public.sh`, then tag origin HEAD
  so release CI builds the public artifacts.
